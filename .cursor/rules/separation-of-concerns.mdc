---
description: "Dev::Deps layer boundaries — don't leak concerns. Repository resolves, Integration installs, Lockfile serializes, Orchestrator coordinates. One class = one layer. Integration-specific logic stays in its integration."
alwaysApply: false
---

# Don't leak concerns across layers

Each layer owns a single responsibility. It must not implement, reference, or
even mention in comments concerns that belong to another layer.

**Repository** — resolves an identifier to a pinned Dependency (version + hash).
Does not know about env scoping, installation, or lockfiles.

**Integration** — installs dependencies it is given. Does not filter, resolve,
or decide which deps to install. Receives only deps that should be installed.

**Lockfile** — pure serialization. Reads/writes YAML. Does not resolve, filter,
or install. Internal file routing (which lockfile for which group) is
encapsulated; consumers call `lock(deps)` and `read`.

**Orchestrator** (future) — owns the lifecycle: resolve, lock, env filter,
dispatch to integrations. Cross-cutting concerns like env detection live here.

```ruby
# BAD: repository knows about env
def fetch(id)
  metadata.merge!(env_metadata(id))  # not your job
end

# BAD: integration filters by env
def install_all(dependencies)
  dependencies.each do |dep|
    next if skip_for_env?(dep)  # not your job
  end
end

# BAD: comment mentions a concern the layer doesn't own
# Env scoping is not this layer's concern — the orchestrator handles it.
class BrewRepository < Repository  # don't talk about what you don't own
```

## One class = one layer

A class must not span two layers. If a single class both resolves+locks
dependencies AND installs them, those are two separate responsibilities that
belong in separate classes.

```ruby
# BAD: one class spans resolve and install layers
class DependencyService
  def resolve(declarations)
    # resolves and locks
  end

  def install(lockfile_path)
    # reads lockfile and installs
  end
end

# GOOD: separate classes for separate layers
class DependencyResolver   # resolve + lock layer
class DependencyInstaller  # install layer
```

## Integration-specific logic stays in its integration

If logic is specific to one integration (e.g., Homebrew tap handling), it
belongs in that integration class — not in the orchestrator or a shared service.

```ruby
# BAD: orchestrator handles brew-specific tap logic
class DependencyService
  def resolve(declarations)
    ensure_taps_added(declarations)  # brew concern, not yours
    # ...
  end
end

# GOOD: BrewIntegration owns its own tap handling
class BrewIntegration < Integration
  def install_all(dependencies)
    ensure_taps_added
    # ...
  end
end
```
